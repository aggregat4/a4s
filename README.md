# A4S

A4S is the Aggregat4 monorepo. It contains independently deployable
applications under `apps/` and shared Go packages under `pkg/`.

The initial migration retains the original projects' working layouts and Git
history. Cross-project convergence happens in separate follow-up changes.
